document.querySelectorAll('.learn-more').forEach(button => {
    button.addEventListener('click', () => {
        alert('This is a placeholder for the training module. You can integrate actual training content here.');
    });
});
